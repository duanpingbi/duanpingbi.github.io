I"�<p>#信号量
    .抽象数据类型：
    -&gt;P(): sem减1，if sem &lt; 0 waiting , else continue
    -&gt;V(): sem加1，if sem &lt;= 0 唤醒一个等待的P</p>
<p>
信号量是正数，被保护的变量
    -&gt;初始化完成之后，唯一能够改变一个信号量的值的办法是通过P()和V()
    -&gt;操作必须是原子的，P()能够被阻塞，而V()不会阻塞
<p>

</p></p>
:ET